# Project Agent Instructions

## Mandatory change logging
- Append a new entry only when code or project files were changed.
- Write entries to daily files in `logs_by_date/` using the format `YYYY-MM-DD_<weekday>.log`.
- Daily log files are append-only: do not modify old entries in existing files.
- Each new entry must include current local datetime and a concise list of implemented changes.
